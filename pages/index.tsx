import type { NextPage } from "next";
import Head from "next/head";

import AppLayout from "../component/AppLayout";

const Home: NextPage = () => {
  return (
    <>
      <AppLayout>
        <div>home</div>
      </AppLayout>
    </>
  );
};

export default Home;
